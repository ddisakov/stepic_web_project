 server {
            listen       80;
            server_name  localhost;

            #access_log  logs/localhost.access.log  main;

            
            location / {
                root /home/box/web/public;

            }
                       
            
            location /uploads/ {
                root /home/box/web/;

            }

       }